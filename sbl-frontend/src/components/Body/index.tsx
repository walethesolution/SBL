import React from "react";
import { Box, Grid } from "@mui/material";

export default function Body() {
  return (
    <Box
      sx={{ marginTop: "10px", backgroundColor: "rgba(128, 128, 128, 0.08)" }}
    >
      <Grid>Map</Grid>
    </Box>
  );
}
